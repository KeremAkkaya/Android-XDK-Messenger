<resources>

    <!-- ########## -->
    <!-- Action Bar -->
    <!-- ########## -->

    <style name="ActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="titleTextStyle">@style/ActionBarTitleText</item>
        <item name="background">@color/primary</item>
    </style>

    <style name="ActionBarTitleText" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/text_white</item>
    </style>

    <style name="ActionBarTabText" parent="@style/Widget.AppCompat.ActionBar.TabText">
        <item name="android:textColor">@color/text_white</item>
    </style>

    <style name="ActionBarButton" parent="@style/Widget.AppCompat.ActionButton">
        <item name="android:tint">@color/text_white</item>
    </style>

    <style name="ActionBarOverflowButton" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/text_white</item>
    </style>

    <style name="BaseMessengerTheme" parent="Theme.XdkUi.Base.Light">
        <item name="actionBarStyle">@style/ActionBar</item>
        <item name="actionBarTabTextStyle">@style/ActionBarTabText</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="actionButtonStyle">@style/ActionBarButton</item>
        <item name="actionOverflowButtonStyle">@style/ActionBarOverflowButton</item>

        <!--EditText cursor; floating action button -->
        <item name="colorAccent">@color/primary</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="android:windowBackground">@android:color/white</item>

        <item name="FourPartItemsListView">@style/FourPartItemsListView</item>
        <item name="ComposeBar">@style/ComposeBar</item>
        <item name="AddressBar">@style/AddressBar</item>
    </style>

    <style name="MessengerTheme" parent="BaseMessengerTheme"/>
</resources>